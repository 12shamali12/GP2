# Hoist transitive deps to the top of node_modules so direct imports of
# packages we use but don't declare (e.g. express, pulled in by
# @nestjs/platform-express) keep resolving in production after
# `pnpm prune --prod`. Without this, pnpm's strict layout breaks any
# phantom-dependency import that worked in local dev.
shamefully-hoist=true
